# TEMPORARY install-resilience settings for a slow/flaky connection.
# Forces sequential downloads so large binaries (next, swc) don't share
# bandwidth and time out. Remove (or tune) before shipping to buyers.
fetch-timeout=1200000
fetch-retries=10
fetch-retry-maxtimeout=300000
fetch-retry-mintimeout=20000
network-concurrency=1
